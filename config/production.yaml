# ===== PRODUCTION CONFIGURATION =====

debug: false
environment: production

api:
  host: "0.0.0.0"
  port: 8000
  reload: false
  workers: 4

logging:
  level: "INFO"

fusion:
  cache_responses: true
  cache_ttl: 3600
